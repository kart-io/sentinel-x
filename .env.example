# Sentinel-X 环境变量配置示例
#
# 说明：
# - 这是环境变量配置示例文件，不包含实际的敏感信息
# - 复制此文件为 .env 并填入真实值：cp .env.example .env
# - .env 文件已在 .gitignore 中，不会被提交到版本控制系统
# - 环境变量会覆盖 YAML 配置文件中的对应值
# - 环境变量命名规则：<APP_NAME>_<CONFIG_KEY>（使用下划线分隔，全大写）
# - 配置键中的点号(.)和横杠(-)都会被转换为下划线(_)
#
# 安全建议：
# 1. 所有密码和密钥必须使用强随机值，不要使用示例值或弱密码
# 2. JWT 密钥最少 64 个字符，使用加密安全的随机生成器
# 3. 生产环境必须设置所有必需的环境变量
# 4. 定期轮换密钥和密码（建议每 90 天）
# 5. 使用密钥管理服务（如 HashiCorp Vault、AWS Secrets Manager）存储敏感信息

# ==============================================================================
# Sentinel API Server (sentinel-api) 配置
# ==============================================================================

# JWT 认证密钥（最少 64 个字符，用于 HMAC 算法）
# 生成命令: openssl rand -base64 64 | tr -d '\n'
# 示例：此处留空，必须设置实际的强随机密钥
SENTINEL_API_JWT_KEY=

# MySQL 数据库配置
# 主机地址（默认：localhost）
SENTINEL_API_MYSQL_HOST=localhost
# 用户名（必需）
SENTINEL_API_MYSQL_USERNAME=
# 密码（必需，不要使用弱密码）
SENTINEL_API_MYSQL_PASSWORD=
# 数据库名（必需）
SENTINEL_API_MYSQL_DATABASE=

# Redis 配置
# 主机地址（默认：localhost）
SENTINEL_API_REDIS_HOST=localhost
# 密码（如果 Redis 启用了认证，则必需）
SENTINEL_API_REDIS_PASSWORD=

# ==============================================================================
# User Center (user-center) 配置
# ==============================================================================

# JWT 认证密钥（最少 64 个字符，用于 HMAC 算法）
# 生成命令: openssl rand -base64 64 | tr -d '\n'
# 示例：此处留空，必须设置实际的强随机密钥
USER_CENTER_JWT_KEY=

# MySQL 数据库配置
# 主机地址（默认：localhost）
USER_CENTER_MYSQL_HOST=localhost
# 用户名（必需）
USER_CENTER_MYSQL_USERNAME=
# 密码（必需，不要使用弱密码）
USER_CENTER_MYSQL_PASSWORD=
# 数据库名（必需）
USER_CENTER_MYSQL_DATABASE=

# Redis 配置
# 主机地址（默认：localhost）
USER_CENTER_REDIS_HOST=localhost
# 密码（如果 Redis 启用了认证，则必需）
USER_CENTER_REDIS_PASSWORD=

# ==============================================================================
# Sentinel Example Server (sentinel-example) 配置
# ==============================================================================

# JWT 认证密钥（最少 32 个字符，用于 HMAC 算法）
# 生成命令: openssl rand -base64 48 | tr -d '\n'
# 示例：此处留空，必须设置实际的强随机密钥
SENTINEL_EXAMPLE_JWT_KEY=

# ==============================================================================
# 其他可选配置
# ==============================================================================

# 日志级别（可选值: debug, info, warn, error, fatal）
# SENTINEL_API_LOG_LEVEL=info
# USER_CENTER_LOG_LEVEL=info

# 服务器监听地址（可选）
# SENTINEL_API_HTTP_ADDR=:8080
# USER_CENTER_HTTP_ADDR=:8081

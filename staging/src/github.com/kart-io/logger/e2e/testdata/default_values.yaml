log:
  engine: zap
  level: info
  format: json
  output_paths:
    - ./logs/defaults.log

  # 零值轮转配置 (应该应用默认值)
  rotation:
    max_size: 0
    max_age: 0  
    max_backups: 0
    compress: false
    rotate_interval: ""

  otlp_endpoint: ""